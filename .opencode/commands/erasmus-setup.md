---
description: Configure the current repository for the Erasmus OpenCode interaction layer
agent: erasmus
---

Load the OpenCode skill named `erasmus-setup` and follow it.

Repository-specific setup request:

$ARGUMENTS
